[build]
builder = "nixpacks"

[deploy]
# УБРАН startCommand - используем только Procfile
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
